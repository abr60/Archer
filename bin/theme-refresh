#!/bin/bash

# omarchy:summary=Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.config/Archer/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  OMARCHY_THEME_SKIP_BACKGROUND=1 theme-set "$(cat $THEME_NAME_PATH)"
fi
