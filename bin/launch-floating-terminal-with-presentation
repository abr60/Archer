#!/bin/bash
# archer:summary=Launch a floating terminal with the Archer presentation wrapper
# archer:args=<command>
cmd="$*"
exec setsid uwsm-app -- xdg-terminal-exec --app-id=org.archer.terminal --title=Archer -e bash -c "show-logo; $cmd; if (( \$? != 130 )); then show-done; fi"
