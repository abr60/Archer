# Changes require a restart to take effect.

# Ensure Archer and Matugen bins are in the path
export ARCHER_PATH=$HOME/.local/share/Archer
export PATH=$HOME/.config/matugen/bin:$HOME/.local/bin:$ARCHER_PATH/bin:$PATH

# Set default terminal and editor
source ~/.config/uwsm/default

# Activate mise if present on the system
Archer-cmd-present mise && eval "$(mise activate bash --shims)"
