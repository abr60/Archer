/* ==========================================================================
   Rofi Theme Picker — Compact 4x2 Vertical Grid
   ========================================================================== */

@import "matugen.rasi"

configuration {
    show-icons:        true;
    hover-select:      true;
    me-select-entry:   "";
    me-accept-entry:   "MousePrimary";
}

* {
    background-color: transparent;
    text-color:       @on-surface-variant;
    font:             "Maple Mono NF 12";
}

window {
    width:            760px;
    background-color: @surface;
    border:           2px;
    border-color:     @outline-variant;
    border-radius:    4px;
    location:         center;
    anchor:           center;
    padding:          2px;
}

mainbox {
    children: [ listview ];
    padding:  0;
    spacing:  0;
}

inputbar  { enabled: false; }
scrollbar { enabled: false; }

listview {
    columns:       4;
    lines:         2;
    spacing:       2px;
    padding:       2px;
    fixed-columns: true;
    fixed-height:  true;
    dynamic:       true;
    cycle:         true;
}

element {
    orientation:      vertical;
    cursor:           pointer;
    padding:          4px 2px;
    spacing:          2px;
    border-radius:    4px;
}

/* ─── State Modifiers (Direct Selection Fixes) ───────────────────────────── */

/* Reset default selection overlay backgrounds completely */
element normal.normal,
element normal.active,
element selected.normal,
element selected.active {
    background-color: transparent;
    border:           0px;
}

/* Hover / Selected State for Regular Items — Force Text to Blue */
element selected.normal {
    text-color: #268bd2;
}

/* Active Theme State when NOT hovered — Force Text to Green */
element normal.active {
    text-color: #859900;
}

/* Active Theme State when SIMULTANEOUSLY selected/focused on launch — Force Text to Green */
element selected.active {
    text-color: #859900;
}

/* ─── Element Components ─────────────────────────────────────────────────── */

element-icon {
    size:             185px;
    horizontal-align: 0.5;
    vertical-align:   0.5;
    border-radius:    4px;
    /* Force icons to retain their natural colors rather than inheriting text-color overrides */
    text-color:       inherit; 
}

element-text {
    enabled:          true;
    horizontal-align: 0.5;
    vertical-align:   0.5;
    text-color:       inherit;
}